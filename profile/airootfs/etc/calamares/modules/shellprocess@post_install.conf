---
# shellprocess@post_install
# Runs inside the installed system (chroot) after pacstrap

dontChroot: false
timeout: 600   # 10 minutes

script:
    - command: "/bin/bash /etc/calamares/scripts/post-install.sh"
      name: "Configuring installed system..."
