---
# shellprocess@pacstrap
# Runs pacstrap to install Arch Linux base packages into the target mount

dontChroot: true
timeout: 1800   # 30 minutes max for package download + install

script:
    - command: "/etc/calamares/scripts/pacstrap.sh @@ROOT@@"
      name: "Installing Arch Linux base system..."
