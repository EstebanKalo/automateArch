/* Allow the live user to perform any privileged action without authentication */
polkit.addRule(function(action, subject) {
    if (subject.user == "live") {
        return polkit.Result.YES;
    }
});
